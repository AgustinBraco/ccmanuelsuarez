import { TopBar, FormContainer, InfoContainer, MapContainer } from '../components';

const Contact = () => (
	<div>
		<TopBar />
		<FormContainer />
		<InfoContainer />
		<MapContainer />
	</div>
);

export default Contact;
